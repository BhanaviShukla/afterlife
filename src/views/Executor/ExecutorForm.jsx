import { memo } from "react";

const ExecutorForm = memo(({ id, executorId, identityDocumentNumber }) => {
  return <div>Form here</div>;
});

ExecutorForm.displayName = "ExecutorForm";
export default ExecutorForm;
